- if notice
  #global_notice
    .ui.positive.message
      .header = notice
    coffee:
      setTimeout( () ->
        $('#global_notice').fadeOut()
      , 3000)

- if alert
  #global_notice
    .ui.negative.message
      i.close.icon
      .header = alert
    coffee:
      $('#global_notice .close').on 'click', () ->
        $('#global_notice').remove()
