
= sync partial: 'issue_header', resource: @issue, refetch: true

.ui.top.tiered.menu

  a.active.item data-tab='comment'
    i.icon.comments
    = Comment.model_name.human
    .ui.circular.label = @issue.comments.size

  a.item data-tab='calendar'
    i.icon.calendar
    = '日程'

.ui.active.tab data-tab='comment'

  = sync partial: 'comment_row', collection: @issue.comments.includes(:user).asc, refetch: true
  = sync_new partial: 'comment_row', resource: Comment.new, scope: Comment.by_commentable(@issue), refetch: true

  - if policy(:comment).create?
    = render partial: 'projects/comments/form', locals: { record: [@project, @issue, @issue.comments.build], suggestions_path: project_suggestions_path(@project) }

.ui.tab data-tab='calendar'
  .center.aligned
    - if @issue.planned?
      = clndr_tag  multiple_events:[ { start_date: @issue.begin_at, end_date: @issue.due_at } ]
    - else
      = clndr_tag

- content_for :javascripts
  coffee:
    $('.menu .item').tab()
